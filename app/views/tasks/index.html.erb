<h1>Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task), data: { turbo_frame: "task_details" } %>
    </li>
  <% end %>
</ul>

<!-- Turbo Frame for showing task details -->
<%= turbo_frame_tag "task_details" do %>
  <p>ths is frame index <p>
<% end %>